import { default as APP } from 'app';

var Layout = APP.Layout.extend({
	/*
	 // already part of Backbone APP...
	el: "body",

	options: {

	},

	views: new Backbone.Model(),
	*/
});

export default Layout;