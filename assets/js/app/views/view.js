// main
import { default as Parent } from 'app/views/common';
// helpers
import { default as _ } from 'helpers/underscore';


var View = Parent.extend({

	options: {

	}
});

// save in the global namespace
//APP.Views.Default = View;

export default View;
